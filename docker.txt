#-------------Install Docker Desktop on Arch-based distributions
https://docs.docker.com/desktop/install/linux/archlinux/

wget https://download.docker.com/linux/static/stable/x86_64/docker-27.2.1.tgz -qO- | tar xvfz - docker/docker --strip-components=1

sudo mv ./docker /usr/local/bin

https://docs.docker.com/desktop/release-notes/
download the Arch version
from download run
sudo pacman -U ./docker-desktop-x86_64.pkg.tar.zst

Pass
https://docs.docker.com/desktop/get-started/#credentials-management-for-linux-users

#-------------Uninstall Docker Desktop on Arch-based distributions
sudo pacman -Rns docker-desktop

  #Credentials remove
sudo rm -f /usr/local/bin/docker-credential-desktop
rm -f ~/.docker/config.json
mkdir -p ~/.docker
echo '{}' > ~/.docker/config.json

which docker
  # /usr/local/bin/docker
  # remove what shows
    sudo rm -f /usr/local/bin/docker




#-------------Install Docker Egine (console version) on Arch-based distributions
sudo pacman -S docker docker-buildx docker-compose
sudo systemctl enable --now docker
sudo usermod -aG docker $USER


#-------------Uninstall Docker Egine (console version) on Arch-based distributions
sudo pacman -Rns docker
sudo pacman -Rns docker-cli
sudo pacman -Rns docker-buildx
sudo pacman -Rns docker-compose
sudo rm -rf /var/lib/docker


#-------------Images
  #Check its parameters
docker manifest inspect mongo:8 | grep linux
  #or
docker manifest inspect mongo:8.3.4-nanoserver-ltsc2022

  #Pull
docker pull mongo:8

  #Container - make
docker run -d \
  --name mongo8 \
  -p 27057:27017 \
  -e MONGO_INITDB_ROOT_USERNAME=root \
  -e MONGO_INITDB_ROOT_PASSWORD=pass123 \
  mongo:8







